# MySQL — agar password hai to format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Password mein special characters ho to URL-encode karein (e.g. @ -> %40)
DATABASE_URL="mysql://root@127.0.0.1:3306/sales_software"

PORT=4000
